version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: <TASK_DEFINITION>
        LoadBalancerInfo:
          ContainerName: "GolangContainer"
          ContainerPort: 8080
artifacts:
  - files:
      - app/appspec.yaml
      - app/taskdef.json
